# Copy to keystore.properties and fill in your values.
# NEVER commit keystore.properties or your .keystore file.
# Place monm-release.keystore in this same directory (packages/monm-android/android/)

storePassword=YOUR_KEYSTORE_PASSWORD
keyPassword=YOUR_KEY_PASSWORD
keyAlias=monm
storeFile=monm-release.keystore
